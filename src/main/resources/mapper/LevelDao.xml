<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.LevelDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.entity.LevelEntity" id="levelMap">
        <result property="level" column="level"/>
    </resultMap>

	<select id="selectListVO"
		resultType="com.entity.vo.LevelVO" >
		SELECT * FROM level  level
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectVO"
		resultType="com.entity.vo.LevelVO" >
		SELECT  level.* FROM level  level
 		<where> 1=1 ${ew.sqlSegment}</where>
	</select>

    <select id="selectListView"
		resultType="com.entity.view.LevelView" >

		SELECT  level.* FROM level  level
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectView"
		resultType="com.entity.view.LevelView" >
		SELECT * FROM level  level <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	

</mapper>
